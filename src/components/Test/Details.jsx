import React from "react";

const Details = ({ title }) => {
  return (
    <div>
      <h1>Hi , guys!</h1>
      <p>{title}</p>
      <button>Click</button>
    </div>
  );
};

export default Details;
